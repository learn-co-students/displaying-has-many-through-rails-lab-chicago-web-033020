<h1>Appointments: </h1>

<strong> <%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> </strong>
<br>
<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor)%>: <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %>
