<h2>Doctor Show</h2>

<%= @doctor.name %>

<% @doctor.appointments.each do |appt|%><br>
  <%= link_to appt.patient.name, patient_path(appt.patient.id) %> <%=appt.appointment_datetime.strftime("%B %d, %Y at %k:%M")%>
  <%end%>